# sample_pipeline
A Tensorflow transfer learning pipeline

This implementation ingests an image dataset and corresponding labels from TFRecords.  It executes transfering learning using a pre-trained ResNet50 model.
